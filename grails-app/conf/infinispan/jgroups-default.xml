<!--

JGroups TCP PING configraturation for Infinispan

Required system properties:

  * payments.infinispan.tcp.port  - base port (defaults to 46690)

  * payments.infinispan.tcp.initial_hosts - list of hosts in the cluster.  Use the format:

            '10.149.36.32[46690],10.149.36.33[46690]'

    The ports should be the same as payments.infinispan.tcp.port

-->

<config xmlns="urn:org:jgroups"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:org:jgroups file:schema/JGroups-3.2.xsd">

    <TCP
            bind_port="${payments.infinispan.tcp.port:46690}"
            recv_buf_size="${tcp.recv_buf_size:5M}"
            send_buf_size="${tcp.send_buf_size:5M}"
            max_bundle_size="64K"
            max_bundle_timeout="30"
            use_send_queues="true"
            sock_conn_timeout="300"

            timer_type="old"
            timer.min_threads="4"
            timer.max_threads="10"
            timer.keep_alive_time="3000"
            timer.queue_max_size="500"

            thread_pool.enabled="true"
            thread_pool.min_threads="2"
            thread_pool.max_threads="8"
            thread_pool.keep_alive_time="5000"
            thread_pool.queue_enabled="true"
            thread_pool.queue_max_size="10000"
            thread_pool.rejection_policy="discard"

            oob_thread_pool.enabled="true"
            oob_thread_pool.min_threads="1"
            oob_thread_pool.max_threads="8"
            oob_thread_pool.keep_alive_time="5000"
            oob_thread_pool.queue_enabled="false"
            oob_thread_pool.queue_max_size="100"
            />


    <TCPPING
            timeout="3000"
            initial_hosts="${jgroups.tcpping.initial_hosts:localhost[7800]}"
            port_range="2"
            num_initial_members="1"
            />


    <MERGE2
            max_interval="30000"
            min_interval="10000"
            />
    <FD_SOCK/>
    <FD
            timeout="15000"
            max_tries="3"
            />
    <VERIFY_SUSPECT timeout="5000"
            />
    <BARRIER/>
    <pbcast.NAKACK2
            use_mcast_xmit="false"
            discard_delivered_msgs="true"
            />
    <UNICAST/>


    <pbcast.STABLE
            stability_delay="500"
            desired_avg_gossip="5000"
            max_bytes="1m"
            />
    <pbcast.GMS
            print_local_addr="false"
            join_timeout="3000"
            view_bundling="true"
            />

    <UFC
            max_credits="200k"
            min_threshold="0.20"
            />
    <MFC
            max_credits="200k"
            min_threshold="0.20"
            />
    <FRAG2
            frag_size="8000"
            />
    <pbcast.STATE_TRANSFER/>
</config>

